#!/usr/bin/env bash
# logx launcher — generated by install.sh; @LOGX_HOME@ is filled in at install time.
set -euo pipefail
export LOGX_HOME="${LOGX_HOME:-@LOGX_HOME@}"
exec "$LOGX_HOME/venv/bin/python" "$LOGX_HOME/src/logx_cli.py" "$@"
